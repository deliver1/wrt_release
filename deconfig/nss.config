# NSS 内存与固件配置
# 保留 HIGH 模式以获得最大的连接数跟踪能力
CONFIG_NSS_MEM_PROFILE_HIGH=y
CONFIG_NSS_FIRMWARE_VERSION_11_4=y

# SKB Recycler (内核网络包内存回收)
# 这是提升小包转发性能的关键，必须保留
CONFIG_KERNEL_SKB_RECYCLER=y
CONFIG_KERNEL_SKB_RECYCLER_MULTI_CPU=y

# 移除所有 ATH11K (WiFi) 和 Mesh 相关的定义
# 这样可以防止编译时预留几百兆内存给根本不用的 WiFi
