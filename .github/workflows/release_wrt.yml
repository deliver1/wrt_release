name: Release WRT
run-name: Release - ${{ inputs.model }}

on:
  workflow_dispatch:
    inputs:
      model:
        description: 'Device Model'
        required: true
        default: 'redmi_ax6_libwrt'
        type: choice
        options:
          - 'redmi_ax6_immortal'
          - 'redmi_ax6_native'
          - 'v2_immort'
          - 'redmi_ax6_libwrt'

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    strategy:
      matrix:
        # 保持 22.04 以避开 GCC 14 编译冲突
        os: [ubuntu-22.04]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true

      - name: Build System Setup
        run: |
          sudo bash -c 'bash <(curl -s https://build-scripts.immortalwrt.org/init_build_environment.sh)'
          sudo apt -y install libfuse-dev
          sudo apt -y autoremove --purge
          sudo apt -y autoclean

      - name: Set Build Date and Timezone
        run: |
          sudo timedatectl set-timezone "Asia/Shanghai"
          echo "BUILD_DATE=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_ENV

      # 修正后的 Pre Clone 路径 [根据 image_de8b17.png 适配]
      - name: Pre Clone
        run: |
          if [ -f "./wrt_core/pre_clone_action.sh" ]; then
            chmod +x ./wrt_core/pre_clone_action.sh
            ./wrt_core/pre_clone_action.sh ${{ inputs.model }}
          else
            echo "Error: Cannot find pre_clone_action.sh in wrt_core/"
            exit 1
          fi

      - name: Cache Dependencies
        uses: actions/cache@v4
        with:
          path: |
            .action_build/staging_dir
            .action_build/dl
          key: ${{ matrix.os }}-build-${{ hashFiles('**/repo_flag') }}-${{ env.BUILD_DATE }}
          restore-keys: |
            ${{ matrix.os }}-build-${{ hashFiles('**/repo_flag') }}-

      # --- 修复 image_de0b31.png 中的错误 ---
      - name: Refresh the cache
        run: |
          if [ -d ".action_build/staging_dir" ]; then
            echo "正在刷新缓存时间戳..."
            find ".action_build/staging_dir" -type d -name "stamp" -not -path "*/target/*" | while read -r dir; do find "$dir" -type f -exec touch {} + ; done
          else
            echo "缓存目录不存在，跳过刷新。"
          fi

      - name: Build Firmware
        run: |
          chmod +x build.sh
          ./build.sh ${{ inputs.model }}

      - name: Get Kernel Version
        run: |
          echo "KERNEL_VERSION=$(grep -oP '(?<=LINUX_VERSION:=).*' include/kernel-defaults.mk || echo 'unknown')" >> $GITHUB_ENV

      - name: Delete Old Cache
        run: |
          gh cache list --key ${{ matrix.os }}-build-${{ hashFiles('**/repo_flag') }}- --json key --jq '.[].key' | while read -r key; do gh cache delete "$key"; done || true

      - name: Machine Information
        run: |
          echo "--------------------------"
          lscpu | grep -E "name|Thread"
          echo "--------------------------"

      - name: Package OpenWrt Firmware
        if: ${{ inputs.model == 'v2_immort' }}
        run: |
          tar -cvf firmware.tar bin/targets/

      - name: Prepare Release Body
        run: |
          echo "编译完成时间：$(date +'%Y-%m-%d %H:%M')" > release_body.txt
          echo "内核版本：${{ env.KERNEL_VERSION }}" >> release_body.txt

      - name: Release Firmware
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BUILD_DATE }}-${{ inputs.model }}
          files: |
            bin/targets/**/immortalwrt-*.{bin,img.gz}
          body_path: release_body.txt
